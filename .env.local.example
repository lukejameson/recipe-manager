# Local development environment
# Copy to .env for local Docker testing

# Not used locally but required by compose file
DOMAIN=localhost

# Security (use simple values for local dev)
JWT_SECRET=local-development-secret-not-for-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=localdev123

# CORS - the override file sets this, but needed for validation
ALLOWED_ORIGINS=http://localhost:3000

# Data storage - local directory
FOLDER_LOCATION=./data
