# Database (PostgreSQL)
DATABASE_URL=postgresql://recipe_user:recipe_password@localhost:5432/recipe_manager

# JWT Secret (change in production!)
JWT_SECRET=local-development-secret-not-for-production

# Server Port
PORT=3001

# Single User Authentication (hardcoded credentials)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=localdev123
